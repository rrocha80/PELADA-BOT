{
  "name": "pelada-bot",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "reauth": "rm -rf auth && npm run build && npm run start"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "7.0.0-rc10",
    "qrcode-terminal": "*",
    "sqlite3": "*"
  }
}
